# Hermes Agent — Python dependencies
#
# The core agent loop (agent/core.py, agent/tools.py) runs on the Python
# STANDARD LIBRARY ONLY, so it works even if pip install fails on a locked-down
# device. These extras just make life nicer and are safe to skip.

# Nicer HTTP than urllib (optional; core falls back to urllib)
requests>=2.31.0

# Colour + markdown rendering in the terminal (optional)
rich>=13.7.0

# .env loading for keys (optional)
python-dotenv>=1.0.0
